# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`pipeline-as-code` — a project for defining CI/CD pipelines as code. Currently in initial setup; the `git-action/` directory is the intended workspace for GitHub Actions workflows or related tooling.

Update this file once the project structure is established.
